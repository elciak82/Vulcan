 language: python
 python:
    - "3.6"
 # command to install dependencies
 install:
   - pip install -r requirements.txt
   - python -m pip install -U selenium
 before_script: 
   - python NewGrades.py
   - python PrivateData.py.dist
 # command to run tests
 script:
   - python Main.py
  
